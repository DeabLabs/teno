{
	"extends": "tsconfig/node18.json",
	"compilerOptions": {
		"baseUrl": ".",
		"paths": {
			// If you add new aliases, update eslintrc.json as well
			"@/*": ["src/*"]
		}
	},
	"include": ["src/**/*.ts", "tsup.config.ts"],
	"exclude": ["node_modules", "dist", ".turbo"]
}
